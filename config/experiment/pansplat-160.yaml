# @package _global_

defaults:
  - /model: pansplat
  - override /loss: [mse, lpips, mvdepth]

wandb:
  name: pansplat
  tags: [pansplat, mp3d, 160x320]

data_loader:
  train:
    batch_size: 6

dataset:
  image_shape: [160, 320]