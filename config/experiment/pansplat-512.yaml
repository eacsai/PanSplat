# @package _global_

defaults:
  - /model: pansplat
  - override /loss: [mse, lpips, mvdepth]

wandb:
  name: pansplat
  tags: [pansplat, mp3d, 512x1024]

data_loader:
  train:
    batch_size: 1

trainer:
  max_epochs: 5

model:
  weights_path: logs/two-view-mp3d-256/checkpoints/last.ckpt

dataset:
  image_shape: [512, 1024]